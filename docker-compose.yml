services:

  # ----------------------------------------------------------------
  # Polymarket Trading Bot
  # ----------------------------------------------------------------
  trader:
    build: .
    container_name: polymarket-trader
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./logs:/app/logs
